---
# OPTIONS
from: markdown+tex_math_single_backslash
to: beamer
output-file: pandoc_beamer.pdf
data-dir: ${.}/..                             #! custom data directory, the directory containing the filters, defaults, and tempaltes folders
pdf-engine: xelatex
citeproc: true
metadata:
  # bibliography: /Users/ce/3-KNWL/33-Ref/myLibrary.bib  # (commented by sync: plugin injects --bibliography)
cite-method: biblatex # or natbib
toc: true
slide-level: 2
filters:
  - ${USERDATA}/filters/beamer.lua
  - ${USERDATA}/filters/callout.lua
# VARIABLES
variables:
  navigation: horizontal
  fonttheme: "professionalfonts"
  theme: "Antibes"
...
